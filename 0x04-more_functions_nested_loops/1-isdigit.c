#include "main.h"

/**
isdigit - checks for digit 0-9
*
*@c: the function parameter
Return: 1 if c is a digit and 0 otherwise
*/

int isdigit(int c)
{
if (c >= 48 && <= 58)
{
return (1);
}
return (0);
}
