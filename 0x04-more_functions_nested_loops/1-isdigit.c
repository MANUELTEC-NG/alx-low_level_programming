#include "main.h"

/**
*_isdigit - checks for digit 0-9
*
*@c: the function parameter
*
Return: 1 if c is a digit and 0 otherwise
*/

int _isdigit(int c)
{
if (c >= 48 && <= 58)
{
return (1);
}
return (0);
}
